package edu.rice.cs.hpc.traceAnalysis.data;

abstract public class AbstractLoop extends AbstractTraceTreeNode {
	public AbstractLoop(int ID, String name, int depth) {
		super(ID, name, depth);
	}
}
