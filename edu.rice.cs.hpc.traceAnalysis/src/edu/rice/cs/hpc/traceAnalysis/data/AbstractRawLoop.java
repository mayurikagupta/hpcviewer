package edu.rice.cs.hpc.traceAnalysis.data;

abstract public class AbstractRawLoop extends AbstractLoop {
	public AbstractRawLoop(int ID, String name, int depth) {
		super(ID, name, depth);
	}
}
